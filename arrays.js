function double(arr) {
    // use map() to iterate through each array element and multiply it by 2
    // return the new array
    return arr.map(number => number * 2);
}

console.log(double([1,2,3,4,5]));